# Vite frontend — copy to .env.local for local dev
# Production (Render): set VITE_API_URL on geotrade-web env (build-time ARG)

VITE_API_URL=http://localhost:8000/api/v1

# After API deploy:
# VITE_API_URL=https://geotrade-api.onrender.com/api/v1
